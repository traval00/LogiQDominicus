// src/App.tsx (stable baseline)
import React from "react";

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "ui-sans-serif, system-ui" }}>
      <h1 style={{ fontSize: 28, marginBottom: 8 }}>
        ✅ LogiqSignals front-end is running
      </h1>
      <p>If you see this, the Vite + React setup is healthy.</p>
      <p>Next: we’ll re-enable the tabs and data.</p>
    </div>
  );
}
